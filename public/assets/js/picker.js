$(document).ready(function () {
    $('#datepicker').datetimepicker({
        format: 'YYYY-MM-DD', // Atur format tanggal yang diinginkan di sini
        locale: 'id', // Atur lokal bahasa Indonesia
    });
});
